import styled from 'styled-components';

export const Container = styled.div`
    max-width: 200px;
    border-radius: 50%;
    overflow: hidden;
    img {
        max-width: 100%;
        display: block;
    }
`